openapi: 3.0.3

info:
  title: GoPlay Management API
  description: REST API for GoPlay application
  version: 1.0.0

servers:
  - url: http://localhost:8080/v1
    description: Development server

paths:
  /info:
    get:
      operationId: getInfo
      summary: Getting app info
      tags:
        - Info
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InfoResponse"

components:
  schemas:
    InfoResponse:
      type: object
      required:
        - name
        - version
      properties:
        name:
          type: string
        version:
          type: string